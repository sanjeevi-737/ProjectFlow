# API base URL — required when frontend & backend are on different origins
# In dev, Vite proxy handles this so you can leave it commented out.
# On Vercel/Netlify/Railway, set this to your backend's URL + /api
#   e.g. VITE_API_URL=https://projflow-api.onrender.com/api
# VITE_API_URL=/api
